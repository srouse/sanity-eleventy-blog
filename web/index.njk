---
layout: layouts/home.njk
tags:
  - nav
navtitle: Home
---

<h1>{{ homePage.title }}</h1>

<h1>Latest Posts</h1>

{% set postslist = collections.myPosts %}
{% include "postslist.njk" %}

<p>More posts can be found in <a href="{{ '/posts/' | url }}">the archive</a>.</p>

{%- for post in posts -%}
  <li>{{ post.title }} {{ post.url }}</li>
{%- endfor -%}

{%- for post in collections.myPosts -%}
  <li>{{ post.title }} {{ post.url }}</li>
{%- endfor -%}

<scw-card
  style="max-width: 600px;">
  <img
    src="https://clubk9.com/wp-content/uploads/2018/04/Depositphotos_4953129_original-1-256x256.jpg"
    alt="img alt text" />
  <sup>Category</sup>
  <h1>Heading</h1>
  <p>a description</p>
</scw-card>

<ul class="nav">
  {%- for nav in collections.nav | reverse -%}
    <li 
      class="
        nav-item
        {% if nav.url == page.url %}
          nav-item-active
        {% endif %}">
      <a href="{{ nav.url | url }}">
        {{ nav.data.navtitle }}
      </a>
    </li>
  {%- endfor -%}
</ul>